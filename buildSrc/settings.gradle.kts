pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

//rootProject.name = "com.github.7hens.ezgradle.gradle.plugin"
//
//include(":ezgradle-bom")
//project(":ezgradle-bom").projectDir = file("../ezgradle-bom")
