jdk:
  - openjdk17
#before_install:
#  - echo "Before install"
#install:
#  - echo "Running a custom install command"
#  - chmod +x ./gradlew
#  - ./gradlew clean -Pgroup=com.gitlab.7hens.ezgradle -Pversion=$VERSION -xtest -xlint publishToMavenLocal
env:
  EXCLUDES_SAMPLES: true