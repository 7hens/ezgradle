jdk:
  - openjdk17
before_install:
  - echo "Before install"
install:
  - echo "Running a custom install command"
  - ./gradlew clean -Pgroup=com.github.7hens.ezgradle -Pversion=$VERSION -xtest -xlint publishToMavenLocal